import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.barbeariamachado.gestao',
  appName: 'Machado Gestão',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
